import mainInfo from "/data/mainInfo";

const initialState = {
  basicInfo: {},
  mainInfo
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
